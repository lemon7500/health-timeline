# 病程日历 1.0.2 发布前自查报告

## 结论

已完成源码级崩溃路径、数据库事务、附件写入、提醒状态机、备份恢复和重复操作审查。已发现并修复确定性缺陷；当前自动化检查为 0 错误，但仍必须完成华为与荣耀真机测试后才能作出设备兼容性结论。

## 已加强的数据保护

- 数据库不再允许破坏性降级迁移；缺少迁移或完整性异常时停止打开，不自动清库。
- 数据库密钥写入必须同步成功；密钥材料不完整时停止打开现有数据。
- SQLCipher 数据库使用 WAL、`synchronous=FULL` 和启动 `quick_check`。
- 1.0.0 → 1.0.1 使用 Room 1→2 非破坏性迁移，历史记录保留。
- 1.0.1 → 1.0.2 不修改数据库结构，仍使用 Room 版本 2，不删除或重建任何表。
- 1.0.2 与 1.0.1 使用相同 `applicationId` 和相同发布证书，可直接覆盖安装并保留应用私有数据。
- 数据写入、附件导入、备份和恢复共享互斥锁，避免并发交叉修改。
- 附件使用临时文件、`fsync`、格式验证、100 MB 限制和 SHA-256 后才登记入库。
- 备份恢复先验证密码、版本、字段、枚举、日期、外键、路径、大小及 SHA-256。
- 恢复附件先安装到独立目录，最后以单个数据库事务切换引用；进程中断不会让当前数据库引用半成品。
- 重复复查广播不会重复推进日期；只有用户完成或跳过后才计算下一次。
- 用药打卡使用数据库唯一索引防重复；数据库迁移会保留一条既有重复记录。
- 重复点击保存有提交中保护；保存失败时编辑框保留，可重试。
- 损坏或缺失的图片/PDF 显示错误提示，不让查看器异常退出。

## 自动化验证

- 16 个 JVM 单元测试：16 通过，0 失败，0 跳过；新增搜索范围、空关键字和排序测试。
- Android Lint：0 错误；剩余提示均为依赖升级、KTX 写法或存储空间 API 建议。
- 3 个 Android 设备测试已成功编译：数据库 1→2 迁移、重复复查投递、重复用药打卡。
- Release R8 压缩构建成功。
- APK v2 签名验证成功，RSA 4096 位，证书 SHA-256 为 `f22314ed12d22ec195b6528df1266e2a14c3caeea21efcb13c431c5a92cc7000`。
- APK 没有 `INTERNET` 权限；最低 API 26，目标 API 37。
- 1.0.2 APK 包名为 `com.healthtimeline.app`、`versionCode=3`、`versionName=1.0.2`，与 1.0.1 证书 SHA-256 完全一致。

## 尚不能由当前环境证明的事项

- 当前没有连接 Android 模拟器、华为或荣耀真机，设备测试只能确认编译成功，尚未实际执行。
- MagicOS 的电池优化、自动启动和后台限制由具体机型/系统版本决定，必须按 `DEVICE_QA_CHECKLIST.md` 真机验证。
- 任何应用都无法对硬件损坏、系统文件系统故障、用户卸载或未备份的恢复出厂设置承诺零数据丢失；应定期导出加密备份并在另一设备验证可恢复。
